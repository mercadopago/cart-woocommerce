const InputHelper = ({ isVisible, type, message, inputId, id, dataMain }) => (
  <input-helper
    isVisible={isVisible}
    type={type}
    message={message}
    input-id={inputId}
    id={id}
    data-main={dataMain}
  ></input-helper>
);

export default InputHelper;
