const InputHelper = ({ isVisible, message, inputId, id }) => (
  <input-table
    isVisible={isVisible}
    message={message}
    input-id={inputId}
    id={id}
  ></input-table>
);

export default InputHelper;
