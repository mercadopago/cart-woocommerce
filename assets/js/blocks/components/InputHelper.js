const InputHelper = ({ isVisible, message, inputId, id, dataMain }) => (
  <input-table
    isVisible={isVisible}
    message={message}
    input-id={inputId}
    id={id}
    data-main={dataMain}
  ></input-table>
);

export default InputHelper;
