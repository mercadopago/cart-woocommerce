const InputHelper = ({ isVisible, message, inputId, id }) => (
  <input-table
    isVisible={isVisible}
    message={buttonName}
    input-id={columns}
    id={id}
  ></input-table>
);

export default InputHelper;
